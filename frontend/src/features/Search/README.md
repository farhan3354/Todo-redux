# Search Module (`src/features/Search`)

This module handles finding places to stay.

## Components to Implement:
1.  **FilterBar.jsx**:
    - Price range slider.
    - Type of place (Entire home, Room, etc.).
    - Amenities checkboxes (Wifi, Pool, Kitchen).

2.  **SearchResultsPage.jsx**:
    - Display results based on search query.
    - Toggle between List View and Map View.

3.  **MapComponent.jsx**:
    - Google Maps or Leaflet integration to show listing pins.
